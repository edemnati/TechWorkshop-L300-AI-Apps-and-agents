name: Run Customer Loyalty Agent

on:
  push:
    paths:
      - 'src/app/agents/customerLoyaltyAgent_initializer.py'
      - 'src/app/tools/discountLogic.py'
      - 'src/prompts/CustomerLoyaltyAgentPrompt.txt'
  pull_request:
    paths:
      - 'src/app/agents/customerLoyaltyAgent_initializer.py'
      - 'src/app/tools/discountLogic.py'
      - 'src/prompts/CustomerLoyaltyAgentPrompt.txt'
  workflow_dispatch:

jobs:
  run-agent:
    runs-on: ubuntu-latest
    env:
      AZURE_PROJECTS_ENDPOINT: ${{ secrets.AZURE_PROJECTS_ENDPOINT }}
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v4
        with:
          python-version: '3.11'

      - name: Ensure jq is available (optional)
        run: |
          sudo apt-get update
          sudo apt-get install -y jq
        shell: bash

      - name: Login to Azure using service principal (AZURE_CREDENTIALS)
        uses: azure/login@v1
        with:
          creds: ${{ secrets.AZURE_CREDENTIALS }}

      - name: Export AZURE_* environment variables for SDKs (parse AZURE_CREDENTIALS with Python)
        env:
          AZURE_CREDENTIALS: ${{ secrets.AZURE_CREDENTIALS }}
        run: |
          python - <<'PY'
          import os, json, sys
          s = os.environ.get("AZURE_CREDENTIALS")
          if not s:
            print("AZURE_CREDENTIALS is empty", file=sys.stderr)
            sys.exit(1)
          try:
            creds = json.loads(s)
          except Exception as e:
            print("Failed to parse AZURE_CREDENTIALS JSON:", e, file=sys.stderr)
            sys.exit(1)
          client_id = creds.get("clientId") or creds.get("client_id")
          tenant_id = creds.get("tenantId") or creds.get("tenant_id")
          client_secret = creds.get("clientSecret") or creds.get("client_secret")
          subscription_id = creds.get("subscriptionId") or creds.get("subscription_id") or ""
          env_path = os.environ.get("GITHUB_ENV")
          if not env_path:
            print("GITHUB_ENV not available", file=sys.stderr)
            sys.exit(1)
          with open(env_path, "a") as f:
            if client_id:
              f.write(f"AZURE_CLIENT_ID={client_id}\n")
            if tenant_id:
              f.write(f"AZURE_TENANT_ID={tenant_id}\n")
            if client_secret:
              f.write(f"AZURE_CLIENT_SECRET={client_secret}\n")
            if subscription_id:
              f.write(f"AZURE_SUBSCRIPTION_ID={subscription_id}\n")
          print("Exported AZURE_* variables to GITHUB_ENV")
          PY
        shell: bash

      - name: Upgrade pip and install Python dependencies (ensure required imports available)
        run: |
          python -m pip install --upgrade pip
          if [ -f requirements.txt ]; then
            pip install -r requirements.txt
          else
            pip install --upgrade \
              python-dotenv \
              azure-identity \
              azure-ai-projects \
              azure-ai-agents \
              azure-search-documents
          fi

          if [ -f setup.py ] || [ -f pyproject.toml ]; then
            pip install -e .
          fi
        shell: bash

      - name: Create .env from ENV repository secret (kept only in workspace) and restrict permissions
        run: |
          # write the ENV secret into .env and src/.env (do not print it)
          printf '%s\n' "${{ secrets.ENV }}" > .env
          mkdir -p src
          printf '%s\n' "${{ secrets.ENV }}" > src/.env
          chmod 600 .env src/.env
        shell: bash

      - name: Export variables from .env into job environment (GITHUB_ENV) and ensure SEARCH_ENDPOINT exists
        run: |
          python - <<'PY'
          import os, sys
          try:
            from dotenv import dotenv_values
          except Exception as e:
            print("python-dotenv not available:", e, file=sys.stderr)
            sys.exit(1)

          env_path = os.path.join(os.getcwd(), ".env")
          if not os.path.exists(env_path):
            print(".env not found, nothing to export", file=sys.stderr)
            sys.exit(1)

          # Read .env into a dictionary
          raw_values = dotenv_values(env_path)  # preserves keys, returns values as strings (may include quotes)

          # Helper to clean values: strip surrounding quotes and whitespace
          def clean_val(v):
            if v is None:
              return None
            s = str(v).strip()
            if len(s) >= 2 and ((s[0] == s[-1] == '"') or (s[0] == s[-1] == "'")):
              s = s[1:-1]
            return s

          values = {k: clean_val(v) for k, v in raw_values.items()}

          gh_env = os.environ.get("GITHUB_ENV")
          if not gh_env:
            print("GITHUB_ENV not available", file=sys.stderr)
            sys.exit(1)

          # Write all cleaned values to GITHUB_ENV so libraries see them as real environment variables.
          with open(gh_env, "a") as f:
            for k, v in values.items():
              if v is None:
                continue
              # Escape newlines for GITHUB_ENV format
              v_str = v.replace("\n", "\\n")
              f.write(f"{k}={v_str}\n")

          # Accept multiple alternative names for the search endpoint; prefer SEARCH_ENDPOINT
          search_keys = ["SEARCH_ENDPOINT", "AZURE_SEARCH_ENDPOINT", "SEARCH_URL"]
          search_val = None
          for key in search_keys:
            if values.get(key):
              search_val = values.get(key)
              # ensure canonical var name exists in environment
              with open(gh_env, "a") as f:
                f.write(f"SEARCH_ENDPOINT={values.get(key).replace('\\n','\\\\n')}\n")
              break

          if not search_val:
            print("ERROR: SEARCH_ENDPOINT not found in .env (ENV secret). Check for SEARCH_ENDPOINT or AZURE_SEARCH_ENDPOINT in your ENV secret.", file=sys.stderr)
            sys.exit(1)

          print("Exported variables from .env to GITHUB_ENV (SEARCH_ENDPOINT present)")
          PY
        shell: bash

      - name: Add src to PYTHONPATH for local imports
        run: |
          echo "PYTHONPATH=${{ github.workspace }}/src" >> $GITHUB_ENV
        shell: bash

      - name: Show installed packages and debug imports (robust)
        run: |
          python -m pip show python-dotenv azure-identity azure-ai-projects azure-ai-agents azure-search-documents || true
          python - <<'PY'
          import os
          try:
            from importlib.util import find_spec
            ok = find_spec('agent_processor') is not None
          except Exception:
            import pkgutil
            ok = pkgutil.find_loader('agent_processor') is not None
          print('agent_processor importable:', ok)
          print('PYTHONPATH=', os.environ.get('PYTHONPATH'))
          print('AZURE_PROJECTS_ENDPOINT=', os.environ.get('AZURE_PROJECTS_ENDPOINT'))
          # do not print secret values such as SEARCH_ENDPOINT or SEARCH_KEY
          PY
        shell: bash

      - name: Run customerLoyaltyAgent initializer
        env:
          AZURE_PROJECTS_ENDPOINT: ${{ env.AZURE_PROJECTS_ENDPOINT }}
          AZURE_CLIENT_ID: ${{ env.AZURE_CLIENT_ID }}
          AZURE_TENANT_ID: ${{ env.AZURE_TENANT_ID }}
          AZURE_CLIENT_SECRET: ${{ env.AZURE_CLIENT_SECRET }}
          AZURE_SUBSCRIPTION_ID: ${{ env.AZURE_SUBSCRIPTION_ID }}
          SEARCH_ENDPOINT: ${{ env.SEARCH_ENDPOINT }}
          SEARCH_KEY: ${{ env.SEARCH_KEY }}
          INDEX_NAME: ${{ env.INDEX_NAME }}
        run: |
          python --version
          python -c "import os; print('AZURE_PROJECTS_ENDPOINT=', os.environ.get('AZURE_PROJECTS_ENDPOINT'))"
          python -c "import os; print('SEARCH_ENDPOINT set=', bool(os.environ.get('SEARCH_ENDPOINT')))"
          python src/app/agents/customerLoyaltyAgent_initializer.py
        shell: bash
